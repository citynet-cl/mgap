<%= simple_form_for(@tipo_tarea, :html => { :class => "form-horizontal" }) do |f| %>
  <% if @tipo_tarea.errors.any? %>
    <div class="alert alert-danger alert-dismissable">
      <button type="button" class="close" data-dismiss="alert" aria-hidden="true">&times;</button>
      <h4>No se pudo registrar este Tipo de Tarea porque se encontró <%= @tipo_tarea.errors.count %>
	      <% if @tipo_tarea.errors.count==1 %> 
		      <%= render text: "error" %>
	      <% else  %>
		      <%= render text: "errores" %>
	      <% end %>
      </h4>

      <ul>
      <% @tipo_tarea.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="form-group">
    <%= f.label :nombre, :class => "col-lg-2 control-label" %>
    <div class="col-lg-10">
      <%= f.text_field :nombre, :class => "form-control" %>
    </div>
  </div>
  <div class="form-group">
	  <div class="col-lg-offset-2 col-lg-10">
		  <%= f.submit :class => "btn btn-primary", :value => "Guardar Tipo de tarea" %>
    </div>
  </div>
<% end %>
