<%= content_tag :h2, "Tareas segun Otros" %>

<% @tareas_o.keys.sort.each do |otro| %>
	<%= content_tag :h3, Otro.find(otro).nombre.humanize %>

	<div class="table-responsive">
		
	<table class="table table-striped table-hover">
		<thead>
	<tr>
	<th>Cliente</th>
	<th>Desarrollador</th>
	<th>Tarea</th>
            <th>Fecha Registro</th>
            <th>HH</th>
	    <th>Acciones</th>
    </tr>
	</thead>
	<tbody>
	<% @tareas_o[otro].each do |tarea| %>

		<tr>
		            <td><%= tarea.cliente.nombre %></td>
		            <td><%= tarea.user.usuario %></td>
		            <td><%= truncate(tarea.nombre, length: 20) %></td>
		            <td><%= tarea.fecha_registro %></td>
		            <td><%= tarea.hh %></td>
		            <td><%= detalles_link tarea %></td>
		            </tr>

	<% end  %>
	</tbody>
	</table>
	</div>
	<p>
        Descarga 
          <%= link_to "Excel", listados2_path(format: "xls") %>
        </p>
<% end  %>



